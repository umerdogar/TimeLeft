import React from "react";
import { Text } from "react-native";

export const Ionicons = ({ name }: { name: string }) => <Text>{name}</Text>;
export const FontAwesome = ({ name }: { name: string }) => <Text>{name}</Text>;
export const MaterialIcons = ({ name }: { name: string }) => (
  <Text>{name}</Text>
);
